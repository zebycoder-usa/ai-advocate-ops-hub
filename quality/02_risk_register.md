# 02 — Risk Register

Concrete risks found in Phase 0 discovery. Line numbers are against baseline `7821888`. Severity = impact if left as-is. Disposition is either **ACCEPTED** (a recorded owner decision — do not raise as a blocker) or **MUST-FIX** (must be resolved and proven before the final ≥9.0 score).

Owner decisions treated as settled (context for the ACCEPTED items below): the whole named team shares ONE profile inside this internal app by design; a simple internal access gate limited to the known team is acceptable; per-user OAuth is not required; the named team is the internal-trusted capability bank. The one hard content rule that stays: a generated client-facing proposal must never contain a fabricated or unverifiable numeric metric.

| # | Risk | Where (file:line) | Severity | Disposition | Notes / fix direction |
|---|---|---|---|---|---|
| R1 | **CLEval is 24 columns, not 25.** Header defines 24 columns and the writer appends exactly 24 values, while the requirement / Manual-Jobs reference sheet is 25-column. | `Code.gs:23` (header), `Code.gs:218` (appendRow); client row `index.html:1132–1141` | High | **MUST-FIX** | Reconcile against the live CLEval tab and the Manual Jobs sheet (`143AHDs0…`). Confirm the true 25th column, then align header + writer + client payload together. Also most client-facing columns are currently written as empty strings — decide populate-or-drop per column. |
| R2 | **CLEval has no de-duplication / idempotency.** `logCLEval` runs outside the script lock and does an unconditional `appendRow` with no unique key. A retry, double-click, or the normal score-then-rewrite sequence (two client calls) creates duplicate/near-duplicate rows. | `Code.gs:214–230` (no lock; not in `GATE_ACTIONS` `Code.gs:200`); client triggers `index.html:1083` and `index.html:1104` | High | **MUST-FIX** | Add an idempotency key (e.g. assignee+jobLink+rounded-score+day, or a client-generated event id) and an upsert/skip-if-seen guard; consider running under the same `LockService` lock as gate writes. Required by the atomic-logging safety rule. |
| R3 | **Single-login gate trusts a browser-supplied name.** Identity (`name`) is chosen client-side and sent verbatim; the backend trusts it with no token. `forceRelease` checks `ADMINS.indexOf(name)` but `name` is self-asserted, so admin authority is spoofable. | `Code.gs:203` (name from payload), `Code.gs:256` (admin check on self-asserted name); client `index.html:1176`, admins `index.html:1171` / `Code.gs:27` | Medium | **ACCEPTED** (with note) | Owner decision: shared single profile + internal-only gate, no per-user OAuth. Accepted for team-member identity. **Residual to confirm with owner:** admin force-release relies on the same self-asserted name — acceptable inside the trusted team, but note it is not a security control. A lightweight shared gate secret could harden it later without per-user OAuth. |
| R4 | **GET can trigger write / model actions.** `doGet` only blocks `GATE_ACTIONS`; `logCLEval`, `log`, and `claude` are not in that set, so they are reachable via a GET query string, not just POST. | `Code.gs:274–278` (doGet), `Code.gs:200` (GATE_ACTIONS omits logCLEval/log/claude) | Medium | **MUST-FIX** | Route all writes and the `claude` action through POST only (add them to the GET-reject set or invert to an allowlist of read-only GET actions). Prevents a stray link / prefetch from writing rows or spending Claude tokens. |
| R5 | **Stale `$85` rate remnants** contradict the v9.3 `$40/hr` floor. Live UI is unaffected (these paths are the unused GAS fallback + a default that the client always overrides), but they are wrong and misleading. Note: no `$55` remnant exists in current code (searched). | `Code.gs:147` (`SYSTEM_PROMPT` "$85.00/hr … Bid floor is $85/hr"), `Code.gs:227` (`d.bid \|\| "$85/hr"` default) | Low | **MUST-FIX** | Update the fallback `SYSTEM_PROMPT` to the $40 full-service floor and change the `logCLEval` default bid to match (`"$40+/hr"`). Comments at `index.html:546` / help text at `index.html:994` reference $85 only descriptively — leave or clarify. |
| R6 | **SEND threshold is a hardcoded `9.0` literal** repeated across scoring, rendering, verdict, and prompt strings. Changing the bar means editing many sites, risking drift between what the UI shows and what the log records. | `index.html:1037, 1050, 1051, 1130` (logic), `index.html:302, 285` (UI copy), `index.html:1060, 1062, 1094, 1095` (prompts) | Low–Medium | **MUST-FIX** | Introduce a single named constant (e.g. `CL_SEND_THRESHOLD = 9.0`) and reference it everywhere, including interpolated prompt text, so the threshold has one source of truth. |
| R12 | **RESOLVED** (commit `7d8f395`). `runEval()` no longer scores; it renders `evalDecision()` = hard-ban override + deterministic `scoreManual()` /19, with zero model calls. The model is confined to proposal prose (`genProposal`) and the CL scorer/rewrite, and can never produce or override the score/verdict. Proven by 4 acceptance tests (surfaced verdict == `scoreManual()`; crypto = deterministic SKIP; `runEval()` makes no fetch); all 14 prior characterization tests passed with zero expectation edits. | `index.html` (`runEval`/`evalDecision`/`renderDecisionCard`) | High | **RESOLVED** |

## Cross-cutting items carried from Phase 0 (not in the six requested, but tracked)
| # | Item | Where | Severity | Disposition |
|---|---|---|---|---|
| R7 | **`staging` branch == `main`; no isolated staging data plane.** A "test tab" inside the live Ops DB is not isolation. | git: `staging`==`main`==`7821888` | High | **MUST-FIX** (prove a separate staging Sheet + Apps Script deployment before any live-data test) |
| R8 | **ActivityLog per-event logging unwired.** Client `logAct` writes only to localStorage; the sheet's login/eval/proposal/copy/logout rows are not produced by the current front end. | `index.html:713`; server writes only AUTO_RELEASE/FORCE_RELEASE `Code.gs:58,258` | Medium | **MUST-FIX or re-scope** (decide whether the advertised audit trail must reach the sheet) |
| R9 | **Session JD/proposal/copy counts are self-reported** from per-browser localStorage. | `index.html:1287` (`seatLogoutPayload`) | Low | **ACCEPTED** (internal-trust) — note only |
| R10 | **Claimed Script/Sheet IDs absent from repo source** (Script `1JhDZUh9…`, Ops DB `1V9h64Zw…`, Manual Jobs `143AHDs0…`). | not present in tracked files | Medium | **MUST-VERIFY LIVE** before trusting any deployment mapping |
| R11 | **Repo lives inside OneDrive** (sync/lock race risk during writes/builds). | repo root path | Low | **ACCEPTED** — note; avoid concurrent sync during commits |

## Correctness gaps found while writing Phase 3 characterization tests
Both are pinned by passing tests that assert the CURRENT (incorrect) behavior, so the refactor phase can fix them under a net. Do NOT fix in the test phase.

| # | Item | Where (file:line) | Severity | Disposition |
|---|---|---|---|---|
| R13 | **RESOLVED** (commit `b86bbe1`). New-client fairness ported into deterministic `scoreManual()` via achievable-max band scaling. For `tenure==='new'`, the unknowable history signals (spend, hire, `$25/hr-avg`) are N/A — removed from earned AND from the achievable max — and the decision bands scale proportionally (`(16/14/12/10)/19 × achievableMax`), so N/A never counts against a new client. Integrity: a new account showing large spend/hires is flagged (`verify client authenticity: inconsistent history`) and those points are not awarded. Non-new clients (`unknown`/`established`, incl. all fixed-price) keep today's absolute `/19` bands byte-identical (proven: established all-signals → `/19` total 18 BOOST unchanged; fixed-price non-new → cliMax 6 total 11 MARGINAL unchanged). Verified fairness helps: a new client on a strong AI job reaches APPLY WITH BOOST vs SKIP/lower. | `index.html` `scoreManual()` (+ `evalDecision`/`renderDecisionCard` surface it) | High | **RESOLVED** |
| R14 | **RESOLVED** (commit `3a02808`). Pakistan is now auto-detected by the hard-ban country regex. Added tokens `pakistan`, `pakistani`, `karachi`, `lahore`, `islamabad`, `rawalpindi`, `faisalabad`, `multan`, `peshawar`, `quetta` (all `\b` whole-word; no added token is a whole-word substring of an allowed US/Europe location, verified by a false-positive-guard test). The previously-pinned "Pakistan not banned" characterization test was updated to expect a hard BAN; India/Bangladesh tests unchanged. | `index.html:504` (`isBanCountry` regex) | Medium | **RESOLVED** |

Note (not a bug): `scoreManual` has no domain→capability inference; the Match score is a manual/model-set signal. This is by design (a human sets the match dropdown), recorded here only so the R12 refactor keeps Match as an explicit input rather than assuming automatic domain detection.

## Open owner decisions (not bugs)
| # | Decision | Where | Disposition |
|---|---|---|---|
| OD1 | **New-client proportional scaling (R13 / Option 2) can let an unproven new client reach APPLY WITH BOOST more easily than the absolute bands would**, which spends more Connects (10–16 vs 6) on a client with no payment history. This is the intended effect of judging a new client on the fraction of knowable criteria, but it has a cost implication. **Default = current behavior (boost allowed).** Owner to confirm, or optionally cap new clients at APPLY STANDARD (no boost) until they have a verified spend/hire history. | `index.html` `scoreManual()` new-client branch (R13, `b86bbe1`) | **OWNER-DECISION** (not MUST-FIX) |

## Honesty gate (non-negotiable content rule)
Any generated client-facing proposal or cover letter must never contain a fabricated or unverifiable numeric metric. Enforced today by prompt rules in `AGENCY_PROOF_BANK` / `PROOF_VOICE_RULES` (`index.html:932`, `index.html:939`) and the CL prompts (`index.html:1060`, `index.html:1062`). Traceability of this gate is a scored rubric dimension in `03_rubric.md`.
